name: aerc

services:

  api:
    build: ./api
    ports:
      - 5001:5000
    environment:
      - AERC_ENV=${AERC_ENV:-docker}
      - DATABASE_URL=${DATABASE_URL}
      - SECRET_KEY=${SECRET_KEY}
      - TZ=Asia/Taipei
      # Email SMTP 設定
      - MAIL_USERNAME=${MAIL_USERNAME}
      - MAIL_PASSWORD=${MAIL_PASSWORD}
      - MAIL_FROM=${MAIL_FROM}
      - MAIL_PORT=${MAIL_PORT}
      - MAIL_SERVER=${MAIL_SERVER}
      - MAIL_FROM_NAME=${MAIL_FROM_NAME}
      - MAIL_STARTTLS=${MAIL_STARTTLS}
      - MAIL_SSL_TLS=${MAIL_SSL_TLS}
      - USE_CREDENTIALS=${USE_CREDENTIALS}
      - VALIDATE_CERTS=${VALIDATE_CERTS}
      - FRONTEND_URL=${FRONTEND_URL}
      - EMAIL_VERIFICATION_EXPIRE_HOURS=${EMAIL_VERIFICATION_EXPIRE_HOURS}
      - PASSWORD_RESET_EXPIRE_HOURS=${PASSWORD_RESET_EXPIRE_HOURS}
      - AUTH_PRIVATE_KEY_PEM=${AUTH_PRIVATE_KEY_PEM}
      - AUTH_PUBLIC_KEY_KID=${AUTH_PUBLIC_KEY_KID}
      # PII 靜態加密金鑰（32 bytes AES-256，hex 或 base64 格式）
      - DATA_ENCRYPTION_KEY=${DATA_ENCRYPTION_KEY}
      # 042 智慧灌溉入口平台 SSO 整合（PORTAL_SSO_SECRET 須為 64 個十六進位字元，形式不符拒絕啟動）
      - PORTAL_SSO_SECRET=${PORTAL_SSO_SECRET}
      - PORTAL_SSO_AUDIENCE=${PORTAL_SSO_AUDIENCE}
      - PORTAL_SSO_ISSUER=${PORTAL_SSO_ISSUER}
      - PORTAL_SSO_MAX_TOKEN_AGE_SECONDS=${PORTAL_SSO_MAX_TOKEN_AGE_SECONDS}
      - PORTAL_WEBHOOK_SECRET=${PORTAL_WEBHOOK_SECRET}
      - PORTAL_ALLOWED_IPS=${PORTAL_ALLOWED_IPS}
    volumes:
      - ./api:/app
      - ./data:/app/data
      - /app/bin
      - /app/include
      - /app/lib
    command: uvicorn src.main:app --reload --host 0.0.0.0 --port 5000
    depends_on:
      - db

  dry-farm:
    build: ./dry-farm
    environment:
      - FAST_API_BASE_URL=${FAST_API_BASE_URL}
      - FAST_API_TARGET=${FAST_API_TARGET}
      - FAST_API_VERSION=${FAST_API_VERSION}
      - VITE_STEP2_OFFLINE_TRAINING=${VITE_STEP2_OFFLINE_TRAINING}
      - VITE_STEP2_OFFLINE_SECTIONS=${VITE_STEP2_OFFLINE_SECTIONS}
    volumes:
      - './dry-farm:/app'
      - '/app/node_modules'
    ports:
      - 3001:3000
    depends_on:
      -  api

  db:
    build: ./db
    environment:
      - POSTGRES_USER=${POSTGRES_USER}
      - POSTGRES_PASSWORD=${POSTGRES_PASSWORD}
      - POSTGRES_DB=${POSTGRES_DB}
      - TZ=Asia/Taipei
      - PGTZ=Asia/Taipei
    command: >
      postgres
      -c timezone='Asia/Taipei'
      -c log_timezone='Asia/Taipei'
    volumes:
      - pg17_data:/var/lib/postgresql/data
    ports:
      - 5433:5432

volumes:
  pg17_data:
  